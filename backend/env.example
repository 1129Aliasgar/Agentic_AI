# Server Configuration
PORT=5000
NODE_ENV=development

# Ollama Configuration
# Make sure Ollama is running on this URL
OLLAMA_API_URL=http://localhost:11434
# Model name (lightweight & fast defaults): try llama3.2:latest or phi3:mini
OLLAMA_MODEL=llama3.2:latest

# Google Gmail API Configuration
# Get credentials from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Gmail API
# 3. Create OAuth 2.0 Client ID credentials
GMAIL_CLIENT_ID=your_gmail_client_id_here
GMAIL_CLIENT_SECRET=your_gmail_client_secret_here
GMAIL_REDIRECT_URI=http://localhost:5000/auth/google/callback
# Path to your Google credentials JSON file (OAuth2 client or service account)
# For OAuth2: Use client_secret_*.json file (requires one-time authorization at /auth/google)
# For Service Account: Use service account JSON with client_email field (works automatically)
GOOGLE_SERVICE_ACCOUNT_PATH=./config/

# Google Calendar API Configuration
# Uses same credentials as Gmail API
GOOGLE_CALENDAR_ID=primary
# Path to your Google Calendar service account JSON file (optional)
GOOGLE_CALENDAR_SERVICE_ACCOUNT_PATH=./config/google-calendar-service-account.json

# OSRM Routing API Configuration
# Public OSRM server (default) or your own instance URL
OSRM_API_URL=http://router.project-osrm.org

# CORS Configuration
# Frontend URL - update if your frontend runs on different port
CORS_ORIGIN=http://localhost:3000

# Frontend URL for OAuth redirects (used in callback)
# Should match your frontend URL where users will be redirected after OAuth
FRONTEND_URL=http://localhost:3000

